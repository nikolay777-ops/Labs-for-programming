#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

int StrSize(char* str);

unsigned long int StoI(char* str, int size);

char* Checker(char* str, int* size);

void InputCheck(char* str, int* size);
